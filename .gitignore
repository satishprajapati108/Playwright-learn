# Node
node_modules/

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
/blob-report/

# Auth session (never commit — it's a live logged-in session)
.auth/

# Env files (secrets)
.env
.env.staging
.env.prod